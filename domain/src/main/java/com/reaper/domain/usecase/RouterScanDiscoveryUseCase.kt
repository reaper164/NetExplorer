package com.reaper.domain.usecase

import com.reaper.domain.repository.RouterScanRepository

class RouterScanDiscoveryUseCase(private val repository: RouterScanRepository) {

    suspend operator fun invoke(){

    }

}